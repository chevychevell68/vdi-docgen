# Statement of Work — {{ customer.name }}

## Scope
This engagement delivers {{ horizon.pods | length }} Horizon pod{% if horizon.pods|length>1 %}s{% endif %} on {{ horizon.version }}.
CPA: {{ 'Enabled' if horizon.cpa_enabled else 'Not required' }}.

{% for p in horizon.pods %}- **{{ p.name }}** ({{ p.site }}, {{ p.region }}), UAG {{ 'internet-facing' if p.uag.internet_facing else 'internal-only' }} × {{ p.uag.count }}, vCenter: {{ p.vcenter }}
{% endfor %}

## Phases & Milestones
{% for ph in engagement.phases -%}
- **{{ ph.name }}** — Billing Milestone: {{ ph.billing_milestone }}
{%- endfor %}

## Assumptions
{% for a in constraints.assumptions -%}
- {{ a }}
{%- endfor %}

## Out of Scope
{% for o in constraints.out_of_scope -%}
- {{ o }}
{%- endfor %}

## Change Log (v{{ engagement.version }})
{% if engagement.changes|length == 0 %}- Initial version.
{% else %}{% for c in engagement.changes -%}
- {{ c }}
{%- endfor %}{% endif %}
