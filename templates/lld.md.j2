# Low-Level Design — {{ customer.name }}

## Pod Topology
{% for p in horizon.pods %}
### {{ p.name }}
- Site: {{ p.site }} ({{ p.region }})
- UAG: {{ 'internet-facing' if p.uag.internet_facing else 'internal-only' }} × {{ p.uag.count }}
- vCenter: {{ p.vcenter }}
{% endfor %}

## Image Build
- Base OS: {{ image_mgmt.os }}
- Instant Clone: {{ 'Yes' if image_mgmt.instant_clone else 'No' }}
- DEM: {{ 'Yes' if image_mgmt.dem else 'No' }}
- FSLogix: {{ 'Yes' if image_mgmt.fslogix.enabled else 'No' }}

## Network, DNS, NTP
- Provided by customer; specifics to be captured during implementation.

## Assumptions & Dependencies
{% for a in constraints.assumptions -%}
- {{ a }}
{%- endfor %}
