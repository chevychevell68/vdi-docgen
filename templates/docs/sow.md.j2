# Statement of Work — {{ project.name or project.client_slug }}

## 1. Overview
- Deployment model: **{{ platform.deployment_model|default('TBD') }}**
- Horizon license: **{{ platform.horizon_license|default('TBD') }}**
- Remote access: **{{ 'Yes' if access.remote_access_required else 'No' }}** (LB: {{ access.load_balancer or 'TBD' }})

## 2. Scope (In)
- Design & planning (Presales discovery, HLD)
- Build & configuration (core Horizon components, per HLD)
- Pilot validation & knowledge transfer
- Documentation (SOW, HLD, LLD, ATP, As-Built, Run Book if required)

## 3. Scope (Out)
- Non-VDI app remediation unless “Application packaging by WWT” is **Yes**
- Endpoint procurement/OS imaging unless **Are we setting up the endpoints?** is **Yes**
- Ongoing operations beyond run-in/KT unless contracted

## 4. Assumptions (from Presales)
- Existing AD: **{{ 'Yes' if identity.ad_existing else 'No' }}**
- Domains: **{{ identity.domains_count or 'TBD' }}**
- DNS/KMS/DHCP provided by customer: **{{ 'Yes' if core.dns or core.kms or core.dhcp else 'TBD' }}**
- Storage: **{{ infra.storage.vendor or 'TBD' }} {{ infra.storage.model or '' }} ({{ infra.storage.type or 'TBD' }}, {{ infra.storage.size_tb or 'TBD' }} TB)**
- Remote access required: **{{ 'Yes' if access.remote_access_required else 'No' }}**
- MFA required: **{{ 'Yes' if security.mfa_required else 'No' }}** (solution exists: {{ 'Yes' if security.mfa_solution_exists else 'No' }})
- Smart card required: **{{ 'Yes' if security.smartcard_required else 'No' }}**
- Workspace ONE Access: **{{ 'Yes' if platform.workspace_one_access else 'No' }}**
- Profile management: **{{ 'Yes' if platform.profile_mgmt_included else 'No' }}**
- Virtual applications: **{{ 'Yes' if platform.virtual_apps_included else 'No' }}**
- Application packaging by WWT: **{{ 'Yes' if platform.app_packaging_by_wwt else 'No' }}**
- GPU required: **{{ 'Yes' if graphics.gpu_required else 'No' }}** (use case: {{ graphics.gpu_use_case or 'N/A' }})

## 5. Capacity Targets
- Hosts: **{{ infra.hosts.count or 'TBD' }}** ({{ infra.hosts.type_config or 'TBD' }})
- Users: **{{ users_sites_usecases.concurrent_users or 'TBD' }} concurrent / {{ users_sites_usecases.total_users or 'TBD' }} total
- Use cases ({{ users_sites_usecases.use_cases_count or 0 }}): {{ (users_sites_usecases.main_use_cases or []) | join(', ') | default('TBD') }}
- Locations: {{ (users_sites_usecases.user_locations or []) | join(', ') | default('TBD') }}
- Datacenters: {{ (users_sites_usecases.datacenters or []) | join(', ') | default('TBD') }}

## 6. Deliverables
- SOW, HLD (this phase)
- LLD, ATP, As-Built, Run Book (post-sales)
- WBS with planned effort
- Training: **{{ 'Yes' if ops_enablement.training_required else 'No' }}** ({{ ops_enablement.training_type or 'TBD' }})
- KT: {{ ops_enablement.knowledge_transfer or 'TBD' }}
- Adoption Services: **{{ 'Yes' if ops_enablement.adoption_services_required else 'No' }}**

## 7. Responsibilities
- **Customer:** Networking, identity, certificates, DNS/DHCP/KMS; endpoint readiness; change control windows
- **WWT:** Architecture/design, install/config, knowledge transfer, documentation

## 8. Risks & Constraints
- HA/DA plan: {{ resilience_io_monitoring.ha_da_plan or 'TBD' }}
- Printing: **{{ 'Yes' if resilience_io_monitoring.local_printing_required else 'No' }}**; USB mass storage: **{{ 'Yes' if resilience_io_monitoring.mass_storage_devices_required else 'No' }}**
- Monitoring tooling: {{ resilience_io_monitoring.monitoring or 'TBD' }}
- Heavy onboarding requirements: **{{ 'Yes' if resilience_io_monitoring.heavy_onboarding_requirements else 'No**
